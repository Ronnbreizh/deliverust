[package]
name = "deliverust"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.50.0", features = ["sync"] }

[dev-dependencies]
tokio = { version = "1.50.0", features = [
  "macros",
  "rt-multi-thread",
  "signal",
  "time",
] }
